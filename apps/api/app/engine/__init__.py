"""API engine package.

Core processing implementation lives in `packages/tablo`.
This package now only retains API-specific engine modules such as
LLM clients, intent/context helpers, MinIO adapters, and step tracking.
"""
