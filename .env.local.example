# ===========================================
# 本地开发环境变量（用于 pnpm dev）
# 复制为 .env：cp .env.local.example .env
# 
# Docker 部署请使用 docker/.env.example
# ===========================================

# ---- OpenAI ----
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4

# ---- PostgreSQL（本地开发使用 Docker 启动）----
# 运行: cd docker && docker compose -f docker-compose.dev.yml up -d
POSTGRES_USER=llmexcel
POSTGRES_PASSWORD=llmexcel
POSTGRES_DB=llmexcel

# ---- MinIO（本地开发使用 Docker 启动）----
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=llm-excel
MINIO_PUBLIC_BASE=storage

# ---- API：JWT / 加密 ----
JWT_SECRET_KEY=dev-secret-key-change-in-production
BCRYPT_ROUNDS=12

# ---- 日志级别 ----
LOG_LEVEL=DEBUG
