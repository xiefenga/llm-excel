# ===========================================
# 生产环境配置模板
# 复制为 .env 并填写实际值：
#   cd docker
#   cp .env.example .env
#   nano .env
# ===========================================

# ---- Docker 镜像配置 ----
# 镜像版本标签（例如 0.1.0 或 latest）
IMAGE_VERSION=latest
# Web 服务端口（默认 8080）
WEB_PORT=8080

# ---- OpenAI ----
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4

# ---- PostgreSQL ----
# 生产环境请使用强密码
POSTGRES_USER=llmexcel
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
POSTGRES_DB=llmexcel

# ---- MinIO ----
# Docker 服务认证（MINIO_ROOT_USER/PASSWORD）
# 生产环境请使用强密码
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD

# API 访问 MinIO 的配置
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=CHANGE_THIS_TO_STRONG_PASSWORD
MINIO_BUCKET=llm-excel
MINIO_PUBLIC_BASE=storage

# ---- API：JWT / 加密 ----
# 生产环境务必设置强随机密钥
# 生成命令：openssl rand -hex 32
JWT_SECRET_KEY=CHANGE_THIS_TO_RANDOM_SECRET_KEY
BCRYPT_ROUNDS=12

# ---- 日志级别 ----
LOG_LEVEL=INFO
